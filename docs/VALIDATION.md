# 验证记录

核实环境：Windows 内核 10.0.17763，当前构建进程不是管理员。检查对象为本地构建的 x86/x64 原生程序；测试证书与账号均为临时合成数据。

## 已执行

两种架构各通过：

- 52 项原生核心/凭据检查：HTTPS 地址、路由和掩码输入边界、完整 SHA-256 固定公钥、Unicode、诊断脱敏、启动前取消，以及 Windows Credential Manager 的保存、读取、信任/地址隔离和删除。
- 10 项真实 TLS 回环集成场景：固定公钥成功登录、不可信证书、错误指纹、错误密码不重试、额外认证不泄露密码、跨来源跳转、私有 CA 正确名称、私有 CA 名称错误、CA 模式跨来源跳转、认证中取消。
- 9 项导入场景：CA 配置文件、指纹配置文件、拒绝混合信任模式、拒绝 HTTP、拒绝缺少信任材料、拒绝缩短指纹、拒绝私钥、直接 PEM CA、直接 DER CA。
- PE 子系统版本 6.1、只导入系统 DLL、没有 Qt/VC 可再发行运行库/UCRT DLL、没有检查表中已知的 Win7 之后才存在的导入函数。
- 中文/英文原生窗口渲染及空账号密码操作的冒烟检查。截图来自本程序自身窗口，不是网页示意图。

Wintun ZIP SHA-256 与官网公布值一致，x86/amd64 DLL 的 Windows Authenticode 签名检查为有效，签名人为 WireGuard LLC。发行过程不修改 Wintun 文件。

本地详细结果生成在 `test-results`。原生程序的运行库被静态合并；动态加载的官方 Wintun 是单独附带的驱动组件，不应被遗漏在“仅系统 DLL”的表述之外。

## GitHub Linux 客户端构建

2026-09-14，提交 `ac55432` 的[独立客户端工作流](https://github.com/sck03/ocserv-openwrt/actions/runs/34851535175)全部成功，生成 Windows x86/x64 的 0.3.0 程序包和对应源码。使用官方 Node.js 24 Actions，两个可执行文件均通过 PE 审计。

从该次工作流下载两个架构的实际产物，核对 GitHub artifact 摘要、程序包 SHA-256、`BUILDINFO.json` 提交及运行库许可证。在上述 Windows 环境中，各自再次通过 52 项核心检查、10 项真实 TLS 回环检查和 9 项配置导入检查。云端交叉编译产物已执行验证，完整 VPN 数据转发和 Win7 仍按下列实机清单验收。

## 服务端管理页

已通过 34 项 Lua 5.1 回归：账号增改删、停用和恢复、旧会话失效、密码哈希不回显、并发修改检测、失败回滚、配置校验、原设置保留与公开 CA 导出。UCI/nixio/occtl 边界使用模拟接口，尚不能替代路由器上真实的 LuCI 与 ocserv 联调。

在 Chrome 152 中对实际前端与模拟 Lua 后端完成 12 项交互检查：账号格式校验、密码显示开关、新增账号、修改用户名并保留密码、修改密码、停用/恢复/删除账号、设置保存确认、配置导出入口及中英文/手机宽度渲染。修正 HTML `pattern` 在新版浏览器 Unicode Sets 模式下的连字符转义；复测未出现 JavaScript 错误。

服务端工作流会重新执行 34 项 Lua 回归，并构建所选 OpenWrt 系列的软件包。客户端工作流执行 PE 审计；测试工具随 artifact 提供，便于在 Windows 实际运行。浏览器交互检查使用本地预览环境执行。

## GitHub Linux 服务端构建

2026-09-14，提交 `c67d666` 的[独立服务端工作流](https://github.com/sck03/ocserv-openwrt/actions/runs/34856261557)全部成功：

| SDK | 架构 | 安装包 |
|---|---|---|
| OpenWrt 24.10.8 | aarch64_generic | 4 个 IPK |
| OpenWrt 25.12.5 | aarch64_generic | 4 个 APK |

每套包含 ocserv 1.5.0、原 LuCI 页面、简体中文翻译和布利杰管理页。云端验证 AArch64 ELF 与动态依赖、实际包产物和 CSS 完整性；下载后再次核对 artifact 摘要、各套 19 个文件的 SHA-256 及官方源码哈希。

另解开 IPK 核对包名、版本、架构及实际安装的 ELF/前端文件。两套 SDK 输出的管理页 JavaScript 和 CSS 一致；用实际打包后的前端与模拟 Lua 后端复测 12 项浏览器交互，全部通过且未出现脚本错误。未在路由器执行 APK 安装或 VPN 通信，不将 SDK 构建和页面复测等同于设备验收。

## 必须在实机联调

以下条件本次未具备，未宣称通过：

1. Windows 7 SP1 x86/x64 的真实启动、证书存储、SHA-2 驱动安装与网络通信。
2. 管理员权限下 Wintun 建立、IP/路由配置、netsh DNS 设置、WFP 规则安装与撤销。
3. N1 ocserv 的实际账号登录、CSTP/DTLS 完整数据转发、双机同账号拒绝第二连接。
4. LAN 访问、DNS/IPv6防绕过，OpenClash 各工作模式，断网重连、网络切换和睡眠唤醒。
5. 连接后的正常断开、窗口关闭和托盘退出所触发的系统网络回滚；托盘真实鼠标交互及 Explorer 重启恢复。
6. 强制结束进程后的回收：WFP 动态会话随进程关闭，本次虚拟网卡关联配置应随设备关闭回收；物理接口上添加的临时绕行路由设计为最多五分钟过期，需实测。
7. 长时间稳定性和吞吐/CPU/内存基准。复用成熟协议核心和静态原生实现是设计选择，不代替基准结果。
8. OPL 定制固件上的 ocserv 1.5.0 依赖解析、真实安装和配置迁移；以及 N1 升级到 25.12 后的 APK 安装与通信。

当前适合进入这些联调步骤，不能仅凭编译和本地回环测试就宣布商业生产验收完成。

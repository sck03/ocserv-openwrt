# 布利杰VPN 中文管理页

页面版本：**0.4.1**。路径：**VPN → 布利杰VPN**。基于 LuCI 登录和权限检查，适用于 ocserv 1.5.0 及后续版本、OpenWrt 25.12。旧页面书签会转到这一管理页。

## 安装

本次 APK 套装仅包含 `ocserv` 和 `luci-app-ocserv-easy`。完整上传到新目录后运行 `sh install.sh`，再重新登录 LuCI。安装成功后会移除原 `luci-app-ocserv` 和其中文翻译，避免重复页面。首次安装、证书和 TUN 说明见 [N1 安装指南](OPENWRT-N1.md)。

包直接依赖 `ocserv`，以及 `luci-compat`、`luci-lib-nixio`、`luci-lib-jsonc`，由当前固件的软件源解析。没有默认管理账号，沿用 LuCI 自身的登录。

## 账号与在线用户

添加账号、改密码、改用户名/组、停用、恢复、删除和断开当前连接。新增密码至少 8 个字符，并需再次输入确认；点击 **保存并生效** 后同时更新永久配置和服务读取的密码文件，不必再找另一处“保存并应用”。修改正常账号时，两处密码留空可保留原记录。页面从不回显密码哈希；不需要分组时可保持默认 `*`。

旧上游页面会把新密码直接保存为明文。安装脚本将这些记录转成 SHA-512 crypt 哈希，保留原密码和已有有效哈希。不能确定的损坏记录标为“需重设密码”；可单独设置新密码，其他账号继续可用。若仅手动安装两个 APK，可在账号页点击“修复旧密码记录”。

改密码、改账号/组或停用时，使用 ocserv 1.5 的会话撤销功能让该账号旧登录失效。普通账号变更不重启其他用户的 VPN。网络参数变更会在检查配置后重启服务。

## 服务设置

端口、地址池、DNS、全量/分流路由、UDP、每账号在线数、断线检测和连接配置中的公开地址。底部提供 **保存并应用**；应用前检查配置，成功后重新读取实际保存值。每账号上限为 1 时，由服务端限制同时在线设备数；断网席位需等待检测释放。

全新安装先添加账号、确认地址池不与 LAN 重叠，再启动服务。证书可通过安装脚本或 `/etc/init.d/ocserv initcerts` 生成。临时配置 `/var/etc/ocserv.conf` 和 `/var/etc/ocpasswd` 由启动脚本生成，不应手动修改。

## VPN 专用上网

新增可选开关，默认关闭。支持已确认的 N1 单网口旁路由拓扑；“开启”自动配置相关网络访问，“关闭并恢复”还原本功能改动。启用前需要 OpenClash 已配置并开启，以及 ocserv 已运行。

管理电脑地址由当前 LuCI 连接读取。页面在应用后重新访问成功才确认；连接失败时后台等待超时并尝试恢复。不会覆盖 OpenClash 的订阅、节点和分流规则。详细边界与故障恢复见 [开关说明](VPN-ONLY-OPENCLASH.md)。

该操作额外要求 firewall、dhcp 和 openclash 的写权限，普通只读登录不能修改。开启期间网络设置按钮暂时禁用，账号管理继续可用。

## 客户端配置

填写客户端实际连接的地址，例如 `192.168.19.253:4443`，点击“下载 .bvpn 连接配置”。默认“首次连接时确认证书”，不依赖本机 CA 文件；也可选择“附带公共 CA 证书”，此时服务器证书必须覆盖填写的域名或 IP。公共 `ca.pem` 仍可单独下载。

员工导入后填写各自的账号密码，也可直接填写地址连接。Windows 客户端的“导出配置”可进一步分发已确认的指纹及连接选项。所有导出都不包含账号密码、CA 私钥或服务端私钥。

## 并发与恢复

账号及服务操作检查配置修订和 UCI 待提交变更，并使用互斥锁。失败时尽量回滚本次改动；不会把其他页面稍后提交的配置直接覆盖成旧快照。普通管理操作备份位于 `/etc/ocserv/easy-backup`，专用上网备份位于 `/etc/ocserv/easy-guard`，均应只由 root 访问。
